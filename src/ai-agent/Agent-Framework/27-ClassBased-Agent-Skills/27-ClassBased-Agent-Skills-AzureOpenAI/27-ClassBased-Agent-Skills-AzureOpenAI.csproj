<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>_30_ClassBased_Agent_Skill</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- OpenAI SDK：提供 OpenAIClient / ChatClient -->
    <!-- ★ 新增：Microsoft.Extensions.AI.OpenAI — 提供 AsIChatClient() 扩展方法
         将 OpenAI.Chat.ChatClient 转换为 IChatClient 接口 -->
    <PackageReference Include="Microsoft.Extensions.AI.OpenAI" Version="10.8.3" />
    <!-- Microsoft.Agents.AI.OpenAI：提供 AgentClassSkill / AgentSkillsProvider / ChatClientAgentOptions / AsAIAgent() -->
    <PackageReference Include="Microsoft.Agents.AI.OpenAI" Version="1.16.0" />
  </ItemGroup>

</Project>
